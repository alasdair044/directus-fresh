{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prestart": "mkdir -p ./database && touch ./database/data.db && chmod 777 ./database/data.db",
    "start": "directus start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "directus": "^11.6.1",
    "sqlite3": "^5.1.7"
  }
}
